# ai-security-agent-demo

Throwaway repo used to demo the AI Security Agent. The `demo-pr` branch
intentionally introduces a SQL injection vulnerability that the agent will
detect and patch automatically when a webhook fires.
